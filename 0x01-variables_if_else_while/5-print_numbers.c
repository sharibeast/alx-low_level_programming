#include <stdio.h>

/**
 * main - Entry point
 * Description: 'function  that prints all single
 * digit numbers of base 10 starting from 0'
 * Return: return integer 0
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
