#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: character param
 * Return: 1 if  is uppercase, 0 if its not uppercase
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
